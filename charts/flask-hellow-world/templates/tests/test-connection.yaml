apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-hellow-world.fullname" . }}-test-connection"
  labels:
    {{- include "flask-hellow-world.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-hellow-world.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
