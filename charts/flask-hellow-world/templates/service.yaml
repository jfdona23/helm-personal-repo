apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-hellow-world.fullname" . }}
  labels:
    {{- include "flask-hellow-world.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-hellow-world.selectorLabels" . | nindent 4 }}
